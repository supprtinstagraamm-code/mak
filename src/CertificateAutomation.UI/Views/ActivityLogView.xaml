<UserControl x:Class="CertificateAutomation.UI.Views.ActivityLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CertificateAutomation.UI.Converters"
             FlowDirection="RightToLeft">

    <UserControl.Resources>
        <conv:NullToVisibilityConverter x:Key="NullVis" />
    </UserControl.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Margin="0,0,0,12">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource TextSubtitle}"
                           Foreground="{DynamicResource AccentBrush}"
                           Visibility="{Binding StatusMessage, Converter={StaticResource NullVis}}" />
                <Button Content="به‌روزرسانی" Style="{StaticResource GhostButton}"
                        HorizontalAlignment="Left" Command="{Binding LoadCommand}" />
            </Grid>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Padding="14,10" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Icon}" FontSize="20"
                                           VerticalAlignment="Center" Margin="0,0,12,0" />

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding ActionLabel}" FontWeight="SemiBold" FontSize="13"
                                                   Foreground="{DynamicResource TextPrimary}" />
                                        <TextBlock Text="{Binding UserName}" FontSize="11" Margin="10,0,0,0"
                                                   Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Details}" Style="{StaticResource TextSubtitle}"
                                               Margin="0,3,0,0" TextWrapping="Wrap"
                                               Visibility="{Binding Details, Converter={StaticResource NullVis}}" />
                                </StackPanel>

                                <TextBlock Grid.Column="2" Text="{Binding When}" FontSize="11"
                                           Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</UserControl>
